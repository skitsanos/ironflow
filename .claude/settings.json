{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/protect-files.sh",
            "timeout": 5,
            "statusMessage": "Checking protected files..."
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "command",
            "command": "printf '%s\n' 'Reminder: answer in English.' 'Use focused tests for ordinary work; do not run tests on every save.' 'For Rust tasks, finish with formatting, module-size policy, exact all-target Clippy, and focused tests.' 'Run scripts/integration_gate.sh only at merge, develop pre-push, release, or explicit-request boundaries.' 'Do not edit protected files such as .env, .env.*, secrets/, or .git/ directly.'"
          }
        ]
      }
    ]
  }
}
