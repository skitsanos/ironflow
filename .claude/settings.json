{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/protect-files.sh",
            "timeout": 5,
            "statusMessage": "Checking protected files..."
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "command",
            "command": "printf '%s\n' 'Reminder: answer in English.' 'For Rust changes, finish by running cargo fmt --all --check, cargo clippy --all-targets -- -D warnings, and cargo test --all.' 'Do not edit protected files such as .env, .env.*, secrets/, or .git/ directly.'"
          }
        ]
      }
    ]
  }
}
