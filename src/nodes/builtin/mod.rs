use crate::nodes::NodeRegistry;

/// Register all built-in nodes into the registry.
/// All nodes have been moved to src/nodes/utility/; this stub remains
/// until builtin/ is removed in the next task.
pub fn register_all(_registry: &mut NodeRegistry) {}
